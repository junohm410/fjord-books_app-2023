<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<div>
  <nav class="page-nav">
    <%= link_to t('views.actions.edit', model: Book.model_name.human), edit_book_path(@book) %> |
    <%= link_to t('views.actions.back_to_list', model: Book.model_name.human), books_path %>

    <%= button_to t('views.actions.delete', model: Book.model_name.human), @book, method: :delete %>
  </nav>
</div>
