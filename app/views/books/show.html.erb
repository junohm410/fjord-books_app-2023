<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('views.common.edit'), edit_book_path(@book) %> |
  <%= link_to t('views.common.back_to_books'), books_path %>

  <%= button_to t('views.common.delete'), @book, method: :delete %>
</div>
